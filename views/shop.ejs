<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Paithani Shops - Paithani Marketplace</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&family=Pacifico&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f5f5f5; /* Soft cream background for Paithani elegance */
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }
        .header {
            background: linear-gradient(to right, #6a5acd, #9370db); /* Royal purple gradient for header */
            color: white;
            padding: 1.5rem;
            text-align: center;
            box-shadow: 0 4px 15px rgba(106, 90, 205, 0.2);
            border-radius: 0 0 8px 8px;
            margin-bottom: 2rem;
            animation: fadeInDown 1s ease-in;
        }
        .header h1 {
            font-size: 28px;
            font-weight: 500;
            margin: 0;
        }
        .content {
            background: #fff5e6; /* Light peach background for Paithani warmth */
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            padding: 2rem;
            display: flex;
            flex-direction: column;
            min-height: 60vh;
            animation: fadeIn 1s ease-in;
        }
        .heading {
            text-align: center;
            margin-bottom: 2rem;
            color: #d4a017; /* Gold for Paithani elegance */
            font-family: 'Pacifico', cursive;
            font-size: 2.5rem;
            font-weight: 400;
            text-shadow: 0 2px 4px rgba(212, 160, 23, 0.3);
            animation: bounceIn 1.5s ease-in-out;
        }
        .shop-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 2rem;
            margin-top: 1rem;
        }
        .shop-card {
            background: white;
            border: 2px solid #d4a017; /* Gold border for Paithani theme */
            border-radius: 10px;
            box-shadow: 0 6px 15px rgba(212, 160, 23, 0.1);
            padding: 1.5rem;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            position: relative;
            overflow: hidden;
            animation: fadeInUp 1s ease-in;
        }
        .shop-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#d4a017" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>') repeat;
            opacity: 0.1;
            z-index: 0;
        }
        .shop-card:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: 0 10px 25px rgba(212, 160, 23, 0.3);
        }
        .shop-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-radius: 6px;
            margin-bottom: 1rem;
            transition: transform 0.3s ease;
            position: relative;
            z-index: 1;
        }
        .shop-card:hover .shop-image {
            transform: scale(1.05);
        }
        .shop-title {
            color: #8b4513; /* Rich brown for Paithani elegance */
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 0.75rem;
        }
        .shop-desc {
            color: #6a5acd; /* Royal purple for description, inspired by Paithani */
            font-size: 0.95rem;
            margin-bottom: 1rem;
            line-height: 1.5;
        }
        .shop-owner {
            color: #878787;
            font-size: 0.85rem;
            margin-bottom: 0.5rem;
        }
        .shop-place {
            color: #388e3c; /* Green for location, subtle Paithani touch */
            font-size: 0.85rem;
            margin-bottom: 1rem;
            font-style: italic;
        }
        .btn {
            background: linear-gradient(to right, #d4a017, #ffd700); /* Gold gradient for buttons */
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 6px;
            font-size: 1rem;
            font-weight: 600;
            transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
            text-decoration: none;
            display: inline-block;
            position: relative;
            z-index: 1;
        }
        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>') repeat;
            opacity: 0.1;
            z-index: -1;
        }
        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 15px rgba(212, 160, 23, 0.4);
            background: linear-gradient(to right, #c68e17, #e6b800);
        }
        .btn-shop {
            background: linear-gradient(to right, #3182ce, #63b3ed); /* Blue gradient for Shop */
            margin-left: 0.5rem;
        }
        .btn-shop:hover {
            box-shadow: 0 6px 15px rgba(49, 130, 206, 0.4);
        }
        .button-container {
            display: flex;
            justify-content: center;
            margin-top: auto;
            padding-top: 2rem;
        }
        .no-shops {
            text-align: center;
            color: #6a5acd; /* Royal purple for no-shops message */
            font-size: 1.1rem;
            padding: 2rem;
            background: #fff5e6;
            border-radius: 8px;
            box-shadow: 0 4px 12px rgba(212, 160, 23, 0.1);
            animation: fadeIn 1s ease-in;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes bounceIn {
            0% { transform: scale(0.8); opacity: 0; }
            50% { transform: scale(1.1); opacity: 1; }
            70% { transform: scale(0.95); }
            100% { transform: scale(1); opacity: 1; }
        }
        @keyframes pulse {
            0% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.05); opacity: 0.9; }
            100% { transform: scale(1); opacity: 1; }
        }
    </style>
</head>
<body class="min-h-screen flex flex-col">
    <header class="header">
        <h1>🧵 Paithani Marketplace</h1>
    </header>

    <main class="flex-grow">
        <div class="container">
            <div class="content">
                <h2 class="heading">Explore Paithani Shops</h2>
                <p class="text-gray-600 text-center mb-4">Browse and discover our collection of exquisite Paithani sarees from various sellers.</p>

                <div class="shop-grid">
                    <% shops.forEach(shop => { %>
                        <div class="shop-card">
                            <% if (shop.image) { %>
                                <img src="<%= shop.image %>" alt="<%= shop.name %>" class="shop-image">
                            <% } else { %>
                                <div class="shop-image" style="background: #e2e8f0; text-align: center; line-height: 200px; color: #4a5568;">No Image</div>
                            <% } %>
                            <h3 class="shop-title"><%= shop.name %></h3>
                            <p class="shop-desc"><%= shop.description || 'No description available.' %></p>
                            <p class="shop-owner">Owner: <%= shop.owner.username %></p>
                            <p class="shop-place">Location: <%= shop.place %></p>
                            <div class="flex justify-between">
                                <!-- Removed "View Details" button, keeping only "Shop" -->
                                <a href="/shop-full/<%= shop._id %>" class="btn btn-shop">Shop</a>
                            </div>
                        </div>
                    <% }); %>
                </div>

                <% if (shops.length === 0) { %>
                    <div class="no-shops">
                        <p>No Paithani shops available yet. Please check back later or contact a seller.</p>
                    </div>
                <% } %>

                <div class="button-container">
                    <a href="/landing" class="btn">Back to Landing</a>
                </div>
            </div>
        </div>
    </main>

    <footer class="footer">
        <p>© 2025 Paithani Marketplace | <a href="#" class="link">Contact Us</a></p>
    </footer>
</body>
</html>